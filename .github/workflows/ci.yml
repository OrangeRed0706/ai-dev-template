name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # === .NET ===
      # - name: Setup .NET
      #   uses: actions/setup-dotnet@v4
      #   with:
      #     dotnet-version: 8.x
      # - name: Restore
      #   run: dotnet restore
      # - name: Build
      #   run: dotnet build --no-restore
      # - name: Test
      #   run: dotnet test --no-build --verbosity normal
      # - name: Format check
      #   run: dotnet format --verify-no-changes

      # === Node.js ===
      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: 22.x
      # - name: Setup pnpm
      #   run: corepack enable && corepack prepare pnpm@latest --activate
      # - name: Install
      #   run: pnpm install --frozen-lockfile
      # - name: Build
      #   run: pnpm build
      # - name: Lint
      #   run: pnpm lint
      # - name: Test
      #   run: pnpm test

      - name: Placeholder
        run: echo "Uncomment the relevant section above for your project"
